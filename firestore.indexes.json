{
  "comment": [
    "Only COMPOSITE indexes belong here.",
    "Firestore creates single-field indexes automatically, in both directions,",
    "and rejects any attempt to declare one explicitly ('this index is not",
    "necessary'). So the notes query (orderBy createdAt desc) needs no entry."
  ],
  "indexes": [
    {
      "collectionGroup": "cards",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "suspended", "order": "ASCENDING" },
        { "fieldPath": "due", "order": "ASCENDING" }
      ]
    }
  ],
  "fieldOverrides": []
}
